{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.publisher_edit'|trans }}
{% endblock %}

{% block body %}
    <body>
    <div class="container">
        <div class="row">
            {% include 'default/_form.html.twig' with {
                page_title: 'title.publisher_edit'|trans({'%id%': publisher.id|default('')}),
                form: form,
                submit_label: 'action.edit'|trans,
                back_to_list_path: 'publisher_index',
            } %}
        </div>
    </div>
    </body>
{% endblock %}