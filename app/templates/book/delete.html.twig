{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.book_delete'|trans({'%id%': book.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="container">
    <div class="row">
    {% include 'default/_form.html.twig' with {
        page_title: 'title.book_delete'|trans({'%id%': book.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'book_index',
    } %}
    </div>
    </div>
    {%  include 'footer.html.twig' %}
{% endblock %}