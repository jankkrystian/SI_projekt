{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.genre_details'|trans({'%id%': genre.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="container">
    <div class="row">
    <h1>{{ 'title.genre_details'|trans({'%id%': genre.id|default('')}) }}</h1>
    {% if genre is defined and genre|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'label.id'|trans }}</dt>
            <dd>{{ genre.id }}</dd>
            <dt>{{ 'label.title'|trans }}</dt>
            <dd>{{  genre.genreTitle }}</dd>
        </dl>
        <p>
            <a href="{{ url('genre_index') }}" title="{{ 'action.back_to_list'|trans }}">
                {{ 'action.back_to_list'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.record_not_found'|trans }}
        </p>
    {% endif %}
    </div>
    </div>
    {%  include 'footer.html.twig' %}
{% endblock %}