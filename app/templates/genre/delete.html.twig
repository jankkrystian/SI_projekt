{% extends 'base.html.twig' %}

{% block title %}
    {{ 'genreTitle.genre_delete'|trans({'%id%': genre.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'genreTitle.genre_delete'|trans({'%id%': genre.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'genre_index',
    } %}

{% endblock %}